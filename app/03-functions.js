exports = typeof window === 'undefined' ? global : window;

exports.functionsAnswers = {

  /**
   * Return the result of invoking the received function using the provided
   * array of arguments that can be of varying or unknown length.
   */
  argsAsArray: function(fn, arr) {

  },

  /**
   * Return the result of invoking the received function in the context
   * of the provided object.
   */
  speak: function(fn, obj) {

  },

  /**
   * Return a "partial" function which takes and argument and invokes fn with
   * the received arguments prepended to the additional argument it receives.
   */
  partial: function(fn, str1, str2) {

  },

  /**
   * Return and array of function closures, each of which each return the
   * result of invoking the received fn with each of the values in the received
   * array.
   */
  makeClosures: function(arr, fn) {

  },

  /**
   * This function receives an unknown amount of number arguments, return the
   * sum of all numbers received.
   */
  useArguments: function() {

  }
};
