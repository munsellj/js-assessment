exports = typeof window === 'undefined' ? global : window;

exports.arraysAnswers = {

  /**
   * Return the number of times an item occurs in an array.
   */
  count: function(arr, item) {

  },

  /**
   * Return an array with all instances of a received item removed.
   */
  remove: function(arr, item) {

  },

  /**
   * Return an array containing only values which are duplicated in the
   * recieved array.
   */
  duplicates: function(arr) {

  }
};

exports.objectsAnswers = {

  /**
   * Alter the context in which the provided method runs.
   */
  alterContext: function(fn, obj) {

  },

  /**
   * Return an array of key / value pairs in the "key: value" format of an
   * object's "own" properties.
   */
  iterate: function(obj) {

  }
};
